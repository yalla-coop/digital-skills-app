import styled from '@emotion/styled';

export const Container = styled('div')`
  padding: ${({ theme }) => `${theme.spacings[5]} ${theme.spacings[4]}`};
`;
export const LogoContainer = styled('div')`
  margin-bottom: ${({ theme }) => theme.spacings[5]};
`;
