import styled from '@emotion/styled';

export const Wrapper = styled.button`
  background: none;
  border: none;
  display: flex;
  align-items: center;
  cursor: pointer;
  padding: 0;
  padding-right: 3px;

  :focus {
    outline: none;
  }
`;
