export const tools = [
  'Google Docs',
  'Google Sheets',
  'Google Slides',
  'Microsoft Word',
  'Microsoft Excel',
  'a digital tool',
];

export const toolDropdownData = tools.map((tool) => ({
  label: tool,
  value: tool,
}));
